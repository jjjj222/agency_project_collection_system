#encoding: utf-8
When /^I fill in the "(\w+)" field with "([^"]+)"/i do |form_name, data|
  pending
end

When /^I press "([^"]+)"/i do |item_name|
  pending
end

Then /^the (\w+) should be "([^"]+)"/i do |item_name, data|
  pending
end
